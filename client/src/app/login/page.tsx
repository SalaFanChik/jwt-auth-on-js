import { LoginForm } from "@/components/LoginForm";

export default function LoginPage() {
    return (
        <main className="w-[100vw] h-screen flex flex-col justify-center items-center bg-gray-200">
            <LoginForm />
        </main>
    )
}